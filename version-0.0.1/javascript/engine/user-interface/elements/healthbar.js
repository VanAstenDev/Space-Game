class HealthBar {
    constructor(startingValue, min, max) {
        this.health = startingValue;
        this.min = min;
        this.max = max;
    }

    display() {
        push();
        
        pop();
    }
}